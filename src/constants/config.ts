/**
 * アプリケーション設定の定数
 * 
 * ハードコードされた値を一元管理し、変更を容易にします。
 */

// =============================================================================
// OSC設定
// =============================================================================

/** OSC送信先ホスト（ローカルホスト） */
export const OSC_HOST = '127.0.0.1'

/** App1のデフォルトOSCポート */
export const DEFAULT_OSC_PORT_APP1 = 9000

/** App2のデフォルトOSCポート */
export const DEFAULT_OSC_PORT_APP2 = 10000

// =============================================================================
// WebSocket設定
// =============================================================================

/** WebSocketサーバーのデフォルトポート */
export const DEFAULT_WS_PORT = 8080

// =============================================================================
// アドレスプレフィックス
// =============================================================================

/**
 * OSC振り分け用のアドレスプレフィックス
 * WebSocketで受信したアドレスがこれらのプレフィックスで始まる場合、
 * 対応するターゲットに振り分けられます。
 */
export const APP_PREFIXES = {
  /** App1へ振り分けるプレフィックス */
  app1: '/app1',
  /** App2へ振り分けるプレフィックス */
  app2: '/app2'
} as const

// =============================================================================
// UI設定
// =============================================================================

/** シーンボタンの数 */
export const SCENE_BUTTON_COUNT = 6

/** ログの最大保持件数 */
export const MAX_LOG_COUNT = 50
